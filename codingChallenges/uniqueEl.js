function uniqueElements(arrayInput){
    let uniqueArr = [];
    for(let i = 0; i<arrayInput.length; i++){
        if(arrayInput.indexOf[i] === -1){
            uniqueArr[i]=arrayInput[i];
        }
        else
    }
}